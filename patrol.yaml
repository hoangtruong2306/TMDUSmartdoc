# Patrol configuration
# Docs: https://patrol.leancode.co/cli-commands/test

app_id: com.example.tdmu_smartdocs

android:
  app_id: com.example.tdmu_smartdocs

# Target test file (relative path — tránh Windows absolute path bug)
targets:
  - integration_test/app_test.dart
